# Copy to .env (gitignored) for local runs. In GitHub Actions these are
# repository secrets: Settings → Secrets and variables → Actions.

# KIE (kie.ai) — media generation API. Validate with: python3 scripts/kie_check.py
KIE_API_KEY=
